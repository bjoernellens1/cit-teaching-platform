# Network Policies for CIT Teaching Platform
#
# These policies provide network isolation between namespaces and
# restrict traffic to only what is necessary for the platform to function.

# =============================================================================
# CIT-AUTH NAMESPACE (Authentik)
# =============================================================================

# Policies removed to resolve connectivity issues.
# previously: authentik-default-deny, authentik-allow-ingress, authentik-allow-egress, authentik-internal

# =============================================================================
# CIT-JHUB NAMESPACE (JupyterHub)
# =============================================================================

# Policies removed to resolve connectivity issues.
# previously: jupyterhub-default-deny, jupyterhub-allow-ingress, jupyterhub-hub-to-proxy, etc.
