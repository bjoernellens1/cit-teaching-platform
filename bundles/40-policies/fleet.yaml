# Fleet bundle configuration for Policies
# Bundle name: cit-policies
name: cit-policies

# Applied to both namespaces
defaultNamespace: ""

# Raw manifests (no Helm chart)
helm:
  chart: ""

# Path to manifests
paths:
  - manifests/

# Apply after all other bundles
dependsOn:
  - name: cit-authentik
  - name: cit-jupyterhub

# Target cluster selection
targets:
  - name: cit-clusters
    clusterSelector:
      matchLabels:
        env: cit-teaching
