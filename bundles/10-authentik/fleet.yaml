# Fleet bundle configuration for Authentik
# Bundle name: cit-authentik
name: cit-authentik

defaultNamespace: cit-auth

helm:
  repo: https://charts.goauthentik.io
  chart: authentik
  version: 2025.12.2
  releaseName: authentik
  valuesFiles:
    - values/authentik-values.yaml

# Include SOPS secret manifests
# Manifests moved to root for automatic discovery
dependsOn:
  - name: cit-crds
  - name: cit-sops-operator

# Target cluster selection
targets:
  - name: cit-clusters
    clusterSelector:
      matchLabels:
        env: cit-teaching
